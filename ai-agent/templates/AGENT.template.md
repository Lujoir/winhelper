---
# ============================================================
# AI 功能 Agent 配置模板  v1.0
# ------------------------------------------------------------
# 用法：复制本文件 → ai-agent/agents/<center|client>/<agent_id>.agent.md
#       逐段填写（**段落一个都不能删**，无内容写「不适用 + 原因」）
#       然后 python ai-agent/tools/publish_agents.py 发布到功能模块
#
# 归档：模板自身变更后，旧版必须移入 templates/archive/AGENT.template.v<版本>.md
# 校验：python ai-agent/tools/verify_agent_configs.py
# ============================================================

# ---- 必填：标识 ----
agent_id: ""              # 全局唯一，小写连字符（如 center-analyze / client-netdoctor-ai）
feature: ""               # 功能中文名（出现在界面上的那个名字）
env: center               # 运行环境，**仅两种取值**：center（中心服务）| client（Windows 客户端）
module: ""                # 所属功能模块 / 代码位置
owner: ""                 # 由哪个 .codebuddy agent 维护
service_target: operator  # 服务对象（准则四）：operator（运维人员）| enduser（使用人本人）
version: "1.0"
status: active            # active | deprecated

# ---- 必填：整合契约（发布到模块后与代码的对应关系）----
code_ref: ""              # 代码位置，格式 文件:符号（如 server-platform/server/ai.py:SYSTEM_PROMPT）
const_ref: ""             # 对应的代码常量名
---

# <feature> · Agent 配置

> **源文件**：`ai-agent/agents/<env>/<agent_id>.agent.md` —— 维护在此
> **本文件是发布副本**（由 `publish_agents.py` 生成，**勿手改**，改动会在下次发布时被覆盖）
> 发布：`python ai-agent/tools/publish_agents.py`　校验：`python ai-agent/tools/verify_agent_configs.py`

## 1. 身份（派生自 Soul）

| 项 | 值 |
|---|---|
| 灵魂来源 | `ai-agent/soul/SOUL.md`（本配置的身份必须与 Soul 一致，不得自造） |
| 角色一句话 | <这个引擎是什么> |
| 立场 | <它站在谁的角度看问题> |

**身份声明**（注入 prompt 的最前置段，就是从 Soul 派生出来的那句"你是谁"）：

```
<实际的 system prompt 首段文本>
```

## 2. 服务对象与表达（准则四）

| 项 | 值 |
|---|---|
| 主要对象 | operator（运维人员）/ enduser（使用人本人） |
| 语言要求 | 技术语言+可核对依据 / 白话+影响+下一步 |
| 输出粒度 | <一条结论里必须包含什么> |

**表达示例**（同一结论的两种说法，标出哪种是本配置采用的）：

- 运维版：`svchost 常驻 CPU 18%，关联 3 个服务；建议核查 Windows Update 计划任务`
- 使用人版：`电脑变慢是系统在后台更新，我帮你清掉缓存能腾出约 30GB`

## 3. 职责与边界

**做**：

- <它负责解决什么问题>

**不做**：

- <明确排除的事，避免越界>

## 4. 输入契约

| 项 | 要求 |
|---|---|
| 必填 | <缺了必须拒绝的字段> |
| 选填 | <可缺省的部分及缺省行为> |
| 限幅 | <单类上限 / 总量预算> |
| 拒绝规则 | <什么输入直接拒，返回什么结构化原因> |

## 5. 输出契约

| 项 | 要求 |
|---|---|
| 结构 | <几段、每段是什么> |
| 必含 | <少了就算不合格的元素> |
| 禁含 | <出现就是污染/违规> |
| 校验点 | <落库前检查什么> |

## 6. 预算与降级（准则二 / ADR-006）

| 项 | 值 |
|---|---|
| 单次超时 | <秒/模型> |
| 模型链长度 | <主 + 备选 = 最坏几次> |
| 最坏耗时 | <计算式 ≥ 结果> |
| 场景预算 | <调用方能等多久> |
| 失败降级 | <失败后调用方拿到什么，绝不空手> |

> **硬约束**：最坏耗时 **必须 ≤** 场景预算。不满足则收超时或砍模型链。

## 7. 隐私边界（准则五）

| 项 | 值 |
|---|---|
| 数据来源 | <输入里的数据从哪来> |
| 是否离机 | <数据是否离开本机；离机则必须说明去了哪> |
| 禁含 | <绝不进入输入/输出的内容> |
| 声明方式 | <隐私声明如何注入 prompt（公共前缀 / 段内声明）> |

## 8. 记忆与上下文（模块 1 / 3）

| 项 | 值 |
|---|---|
| 记忆目录 | `ai-agent/memory/<module_id>/`（或"暂未接入 + 原因"） |
| 落库内容 | <什么进记忆：结论/依据/时间戳> |
| 上下文留存 | <会话增量落哪、保留多久> |
| 清理策略 | <TTL、归档、purge 归属> |

## 9. 触发与优先级（模块 2）

| 项 | 值 |
|---|---|
| 触发类型 | 交互式 / 事件式 / 定时式（可多选） |
| 优先级 | P0 / P1 / P2 |
| 推理留痕 | <步骤如何落 SESSION.jsonl，事后能否回答"凭什么这么判断"> |
| 回退三层 | ①模型链降级：<…> ②阶段降级：<…> ③任务降级：<…> |

## 10. 整合契约

| 项 | 值 |
|---|---|
| 代码位置 | <文件:行/符号> |
| 对应常量 | <代码里的常量名> |
| 加载方式 | <代码如何读到本配置（当前 / 目标）> |
| 一致性校验 | <verify_agent_configs.py 检查什么> |
| 待迁移项 | <配置与代码当前不一致处，记为欠账> |

## 11. 变更记录

| 日期 | 版本 | 变更 | 变更人 |
|---|---|---|---|
| | 1.0 | 初版 | |
