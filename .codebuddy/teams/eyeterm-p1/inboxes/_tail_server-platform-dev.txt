--- 2026-09-18T13:10:18.065Z | from:team-lead | type:message
【WoL 通道定案（用户假设实证）+ 产品行动项】

对照实验（main 会话执行，临时件已清理）：服务器 172.17.5.215 同时发单播+定向广播到同网段在线终端 172.17.90.215:9147——**单播 3/3 秒达，定向广播 0/3 未达**（90s 窗口）。铁证：路径通、本机防火墙无碍，纯粹是三层设备默认禁转 directed broadcast（业界默认）。此前 SRV-116 直发 90.18 的 300s 未恢复与今日 08:30 timer 无效，全部由此解释。

**产品结论**：跨网段唤醒的唯一可行主通道 = wol_relay 同网段中继；直发仅对与服务器同网段的部署形态有效。

行动项（请排期）：
1. **wol 调度器优化（建议）**：对已知跨网段目标（目标机 asset_ip 与服务器不同 /24），跳过 240s 直发观察窗直接从中继起步——省 4 分钟无效等待；直发保留为同网段形态的首选
2. **M720t 的每日 08:30 计划**：建议立即在 wol_schedules 注册（产品调度器自动走「直发→中继升级」链，中继选举会命中 Jun-office-PC）；现有 systemd timer（wol-win13f.timer）对此目标已被证明无效，建议停用防误导（或保留但在台账标注「跨网段无效，以平台调度为准」）
3. 今晚实验的 SRV-116 直发尝试已在审计留痕（powercontrol.wol_direct），台账 SRV-116 语义可注记「跨网段受 directed-broadcast 过滤限制，跨网段场景以 wol_relay 为主通道」

回执后我把结论同步用户。
--- 2026-09-18T13:36:14.721Z | from:api-registrar-dev | type:message
SRV-116 适用边界语义注记已入账，commit 61b9b12（仅台账，+3/-2，条数不变 227）。

按 team-lead 行动项追加，内容完整覆盖：①对照实验定案（单播 3/3 达 vs 定向广播 0/3 达，90s 窗）②此前直发跨网段 300s 未恢复与 08:30 定时无效的归因③跨网段以 wol_relay 为主通道、直发仅同网段有效（method=direct 显式保留）④wol.py direct_applicable 跳直发优化与调度器对齐、ADR-044 增补。

【脱敏执行】你知会原文含两端生产 IP，台账已按脱敏铁律处理——实验描述写「服务器→跨网段目标机（生产 IP 脱敏不录）」，仅保留探测端口 9147（非凭据类）。后续含生产 IP/凭据的知会请预先脱敏或明示「登记时脱敏」。

SRV-116 语义自此完整（响应形状+确认语义+适用边界），Wol 产品化三通道（direct/relay/schedules）登记闭环。
--- 2026-09-18T13:37:07.092Z | from:api-registrar-dev | type:message
口径差异说明：227 正确，你记忆的 226 是 4.1.5 批次（e6e9974）后的快照。+1 增量明细一行：

- **BRG-074** POST /api/filesearch/indexer-deploy —— 文件检索索引器部署引导（2026-09-18 凌晨批次，file-search 4.1.7，主仓 c598e05/子仓 9c3c680，net-doctor-dev 辖区按常设约定知会，commit eb35781 登记；同期 BRG-071 status 三态为既有条目更新行不计条数）

你侧当前口径应同步为：**227 条（SRV 119 / BRG 74 / UPL 18 / EXT 7 / DEP 5）**。另 ADR-044 增补文档脱敏修订（未提交暂存态）收到，随下批 commit 入账即可——台账侧 61b9b12 已是脱敏版，两边口径一致。
--- 2026-09-18T14:23:49.386Z | from:team-lead | type:message
【立项：WoL 双路线架构——平台系统设置加「唤醒模式」开关，方案二中继增强先实现，方案一准入预埋】

## 用户定案（原话要点）
双技术路线同步开发，平台系统设置做开关切换：
- **方案一（准入唤醒）**：画方准入在各终端 VLAN 已部署接口 IP——让画方做一个接口，通过 API 批量传入 IP+MAC 由准入在目标 VLAN 内发唤醒广播。需求已提交画方，最稳定，但**外部依赖未到**。
- **方案二（中继唤醒）**：平台筛选同 VLAN 待开机目标的 IP+MAC → 检索同 VLAN 在线终端 → **随机**选一台代发 → 失败或 5 分钟无在线终端则**换一台继续** → 终态兜底可指定专用代理机。用户定性「可行性很高，先实现这个」。

## 现状与差距（main 分析）
现有 4.1.5 wol.py 已有：调度守护、直发→中继升级链、同 /24 中继选举（last_seen 降序单候选）、wol_schedules/wol_attempts、5 态留痕。与你刚交付的优化（跨网段跳直发）构成基线。**差距**：①无模式开关 ②中继单候选非「随机+多候选轮替」③批量目标支持待核 ④无画方适配层 ⑤无专用代理指定。

## 交付规格

**① 模式开关（系统设置）**
- settings 新键 `wol.wake_mode ∈ {relay(默认), nad}`；console 现有设置区加切换（若平台无系统设置 UI，就在开关机管控页头部加一块小型设置卡，极简两选项+说明）
- nad 模式未配置画方接口时：调度/手动唤醒如实报「画方准入唤醒接口未接入，已回退中继模式执行」并照常走 relay 链——不许哑等
- 接口知会 api-registrar（settings 读写端点按既有形状）

**② 方案二增强（本期主交付）**
1. **中继选举改随机多候选**：同 /24 在线候选集乱序（随机起点），失败按序轮替下一台；每次尝试独立留痕 attempts（含中继 tid）；整轮候选耗尽或全局超窗（用户口径 5 分钟可配）→ give_up
2. **批量目标**：schedule 与手动唤醒均支持多目标（targets[]，逐目标独立走唤醒链，并发上限防风暴——比如同时最多 N=3 条 wol_relay 在途）
3. **专用代理兜底**：schedule/全局配置支持 `pinned_relay`（指定常驻中继终端，如 Jun-office-PC）——候选集为空时用 pinned；pinned 离线照常 give_up 如实
4. **VLAN 语义红线不变**：同 VLAN 判定继续用 /24 近似（无 VLAN 库不推测——铁律）；文档与 UI 文案统一写「同网段」；画方接口未来若返回真 VLAN 信息再升级判定
5. 终端
--- 2026-09-18T14:32:36.856Z | from:team-lead | type:message
【WoL 双路线批追加：VLAN 权威知识库 + 三级检索梯子】

用户指定：VLAN 判定不再只用 /24 近似——权威数据源为用户提供的钉钉表格（main 待用户导出 xlsx 后解析灌入）；检索不到时**用在线终端的网络配置对照**。

设计要求（并入你手头双路线批）：
1. **新表 vlan_kb**（或并入既有知识库结构，你按平台知识库惯例定）：cidr（如 172.17.90.0/24）、vlan_id、区域/位置描述、来源（标注「钉钉表格 20260918 版」）、updated_ts；支持批量导入（main 解析 xlsx 后经导入端点或脚本灌入，导入需幂等+留痕）
2. **三级检索梯子（中继选举与 tracert 标注共用）**：
   L1 vlan_kb 精确匹配（目标 IP 落在某 cidr → 该 VLAN）→
   L2 在线终端网络配置对照（目标 /24 内在线终端的 asset.network 自报网关/掩码/连接名互证，视为同网段候选）→
   L3 /24 近似兜底（现状语义）
   每次选举/标注记录命中层级（kb/config/fallback），attempts 留痕
3. tracert 的「知识库未覆盖」标注逻辑同步用此梯子（net-doctor 消费端知会 net-doctor-dev，纯读取不阻塞你）
4. xlsx 数据未到前：梯子自动落到 L2/L3，行为与现状等价——不阻塞 4.1.8 其它项
5. 端点如新增按约定显式列清单知会 api-registrar

收到回执一句即可。
--- 2026-09-18T14:33:22.237Z | from:team-lead | type:message
【WoL 双路线批规格补充（用户明确）：中继候选仅限 Windows 终端】

在中继选举的所有取数处（随机多候选轮替/pinned_relay/手动唤醒代发列表）加硬过滤：

- **type 字段必须 = Windows**（以 terminals 注册 type 的实际枚举值为准，你实查确认大小写）——其它终端类型（安卓等未来形态）开发模式未定，一律不得当选中继/代发方，也不得进 pinned_relay 候选
- 过滤发生在候选集构建层（不是最终随机抽取时），保证 L1/L2/L3 任意命中层级下候选集都只含 Windows
- 检索结果为空（同 VLAN 无在线 Windows 终端）→ 如实走 give_up/提示，**不跨类型降级**——用户口径：宁可不唤醒，不误选未定形态的终端
- attempts 留痕记录候选数与过滤后数量（便于排障「为何没有中继」）
- ADR-044 增补此约束及理由（其他终端开发模式未定，硬过滤防误选；后续形态定型后由用户决策放开）

与已发的 vlan_kb 三级梯子、随机多候选、批量目标、pinned 代理同批实现。回执一句即可。